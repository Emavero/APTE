# Frontend APTE — copier en .env.local pour le développement.
# Attention : les variables VITE_* sont inlinées dans le bundle et donc
# PUBLIQUES. N'y placez jamais de secret.

# Racine de l'API backend (sans /api, ajouté par le client HTTP).
VITE_API_URL=http://localhost:8000
